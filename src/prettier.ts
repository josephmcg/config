import { Options } from 'prettier'

const config: Options = {
  arrowParens: 'always',
  printWidth: 80,
  singleQuote: false,
  jsxSingleQuote: false,
  semi: true,
  trailingComma: 'all',
  tabWidth: 2,
}

export default config
